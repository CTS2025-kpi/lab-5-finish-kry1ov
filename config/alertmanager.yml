route:
  group_by: ["alertname"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h
  receiver: "n8n-webhook"

receivers:
- name: "n8n-webhook"
  webhook_configs:
  - url: "http://n8n:5678/webhook/alert"
    http_config:
      basic_auth:
        username: admin
        password: admin
